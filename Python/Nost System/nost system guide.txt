run this when checking: 
python main.py -m screen:phone_samsung_galaxy_note_4,portrait,scale=0.3

# when inputting data
x = {"Username-{}".format(username): {"Password" : password}} 
requests.patch(url=url, json = x)

# when deleting data
x = "Username-sdfdsaf/Password"
requests.delete(url=url[:-5] + x + ".json")

